# -*- coding: utf-8 -*-

import pytest

@pytest.mark.django_db()
def test_post_quick_buy(client, load_expected_body, load_input_body, clear_json, deep_diff):
    return


@pytest.mark.django_db()
def test_post_buy(client, load_expected_body, load_input_body, clear_json, deep_diff):
    return


@pytest.mark.django_db()
def test_post_extend(client, load_expected_body, load_input_body, clear_json, deep_diff):
    return


@pytest.mark.django_db()
def test_post_delay(client, load_expected_body, load_input_body, clear_json, deep_diff):
    return